import 'package:flutter/material.dart';

class MapHistoryPage extends StatelessWidget {
  const MapHistoryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Map History Page'),
    );
  }
}
